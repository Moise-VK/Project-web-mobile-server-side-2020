{% extends 'pages/layout.twig' %}

{% block pageContent %}
<main id="main">
    <section class="breadcrumbs">
        <div class="container">

            <div class="d-flex justify-content-between align-items-center">
                <h2>Ticket aanmaken</h2>
                <ol>
                    <li><a href="home">Home</a></li>
                    <li>Evenementen</li>
                    <li>Ticket aanmaken</li>
                </ol>
            </div>
        </div>
    </section>
    <section>
        <div class="col-sm-3"></div>
        <div class="container col-sm-6">
            <!-- CENTER THIS RADIOBUTTON THING -->

                <div class="d-flex justify-content-center" id="eventSelection">
                    <div>
                        <input type="radio" id="existing" name="sortEvent" value="existing">
                        <label for="existing"> Bestaand evenement</label>
                    </div>

                    <div class="ml-5">
                        <input type="radio" id="new" name="sortEvent" value="new">
                        <label for="new"> Nieuw evenement</label>
                    </div>
                </div>

                <div id="existingEvent" class="mt-2" style="display: block;">
                    {% include 'layout/parts/existingEvent.twig' %}
                </div>
            <form action="/create/ticket" method="POST" novalidate enctype="multipart/form-data">
                <div id="newEvent" class="mt-2"  style="display: none;">
                    {% include 'layout/parts/newEvent.twig' %}
                </div>

                <div id="ticketDetails" class="mt-2" style="display: block;">
                    {% include 'layout/parts/ticketDetails.twig' %}
                </div>
            </form>
        </div>
        <div class="col-sm-3"></div>
    </section>
</main>
    <script src="/assets/js/newTicket.js"></script>
{% endblock %}